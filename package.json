{
  "name": "chrome-mcp-router",
  "version": "0.1.0",
  "description": "A resilient MCP proxy for Chrome DevTools that provides automatic reconnection on crash and project-based port resolution",
  "license": "MIT",
  "author": "book000",
  "packageManager": "pnpm@11.21.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/book000/chrome-mcp-router.git"
  },
  "main": "build/index.js",
  "bin": {
    "chrome-mcp-router": "build/index.js"
  },
  "files": [
    "build/"
  ],
  "engines": {
    "node": "26.7.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf build *.tsbuildinfo",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@book000/eslint-config": "1.16.38",
    "@types/node": "24.13.3",
    "eslint": "10.8.1",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
